generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model ArtilheiroBrasileiro {
    id    String @id
    name  String
    team  String
}
